{% extends 'base.html.twig' %}

{% block title %}Commande Confirmée{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="card shadow-lg p-4">
            <h1 class="text-center text-success">✅ Commande Validée</h1>
            <p class="text-center">Merci pour votre achat, {{ app.user.name }} !</p>
            
            <h3 class="mt-4">🛒 Récapitulatif de la commande :</h3>
            <ul class="list-group">
                {% for item in cartItems %}
                    <li class="list-group-item">
                        <strong>{{ item.product.name }}</strong> - {{ item.quantity }} x {{ item.product.price }} € = <strong>{{ item.quantity * item.product.price }} €</strong>
                    </li>
                {% endfor %}
            </ul>

            <h4 class="mt-3">Total payé : <span class="text-danger">{{ total }} €</span></h4>
            <h5>Adresse de livraison : <span class="text-primary">{{ address }}</span></h5>

            <div class="text-center mt-4">
                <a href="{{ path('home') }}" class="btn btn-primary">Retour à l'accueil</a>
            </div>
        </div>
    </div>
{% endblock %}
